name: CI

on:
  push:
    branches:
      - docker-setup

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push frontend image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/3nigma-frontend:latest ./client
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/3nigma-frontend:latest

      - name: Build and push backend image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/3nigma-backend:latest ./server
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/3nigma-backend:latest
