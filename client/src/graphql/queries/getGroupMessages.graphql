query getGroupMessages($groupId: ID!) {
  getGroupMessages(groupId: $groupId) {
    id
    content
    sender {
      id
      username
    }
    timestamp
    isGroupMessage
    groupRecipientId
  }
}